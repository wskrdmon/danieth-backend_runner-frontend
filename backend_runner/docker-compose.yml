version: '3.8'

services:

  redis:
    image: redis:7-alpine
    container_name: runner-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  api_gateway:
    build:
      context: .
      dockerfile: api_gateway/Dockerfile
    container_name: runner-api-gateway
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - redis
      - auth_service
      - tool_executor
    restart: unless-stopped

  auth_service:
    build:
      context: .
      dockerfile: auth_service/Dockerfile
    container_name: runner-auth
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - redis
    restart: unless-stopped

  analysis_service:
    build:
      context: .
      dockerfile: analysis_service/Dockerfile
    container_name: runner-analysis
    ports:
      - "8002:8002"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - redis
    restart: unless-stopped

  tool_registry:
    build:
      context: .
      dockerfile: tool_registry/Dockerfile
    container_name: runner-tool-registry
    ports:
      - "8003:8003"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - redis
    restart: unless-stopped

  tool_executor:
    build:
      context: .
      dockerfile: tool_executor/Dockerfile
    container_name: runner-tool-executor
    ports:
      - "8004:8004"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - TOOL_REGISTRY_URL=${TOOL_REGISTRY_URL}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
      - tool_registry
    restart: unless-stopped

  tool_nmap:
    build:
      context: tools/nmap
      dockerfile: Dockerfile
    image: backend_runner-nmap
    container_name: runner-tool-nmap-builder
    entrypoint: ["echo", "imagen lista"]

  tool_sqlmap:
    build:
      context: tools/sqlmap
      dockerfile: Dockerfile
    image: backend_runner-sqlmap
    container_name: runner-tool-sqlmap-builder
    entrypoint: ["echo", "imagen lista"]

  tool_nuclei:
    build:
      context: tools/nuclei
      dockerfile: Dockerfile
    image: backend_runner-nuclei
    container_name: runner-tool-nuclei-builder
    entrypoint: ["echo", "imagen lista"]

  tool_xsstrike:
    build:
      context: tools/xsstrike
      dockerfile: Dockerfile
    image: backend_runner-xsstrike
    container_name: runner-tool-xsstrike-builder
    entrypoint: ["echo", "imagen lista"]
